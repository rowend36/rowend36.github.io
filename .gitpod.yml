# Commands to start on workspace startup
tasks:
  - name: Setup & Build
    init: npm install
    command: npm start

# Ports to expose on workspace startup
ports:
  - port: 3000
    onOpen: open-preview
    name: rowend36.github.io
    description: Github Portfolio Site
